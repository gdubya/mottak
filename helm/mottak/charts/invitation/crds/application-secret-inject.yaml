apiVersion: spv.no/v1alpha1
kind: AzureKeyVaultSecret
metadata:
  name: mottak-invitation-application-secret-inject
  namespace: mottak
spec:
  vault:
    name: arkivverketvault
    object:
      name: mottak-invitation-application-secret
      type: secret
