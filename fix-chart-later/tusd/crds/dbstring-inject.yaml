apiVersion: spv.no/v1alpha1
kind: AzureKeyVaultSecret
metadata:
  name: mottak-tusd-dbstring-inject
  namespace: mottak
spec:
  vault:
    name: arkivverketvault
    object:
      name: mottak-tusd-dbstring
      type: secret
